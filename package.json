{
  "version": "0.0.1",
  "name": "emoterizer-transformations",
  "dependencies": {
    "imagescript": "LeonChuu/ImageScript",
    "lib-upng": "^3.0.0"
  },
  "type": "module",
  "scripts": {
    "prepare": "npm run build",
    "compile": "tsc",
    "build": "tsc && cp -r resources dist"
  },
  "main": "./dist/index.js",
  "devDependencies": {
    "@types/node": "^18.15.2"
  }
}
